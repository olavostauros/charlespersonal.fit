{
  "name": "charlespersonal-fit",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "type-check": "tsc --noEmit",
    "lint:ts": "tsc --noEmit --strict",
    "validate": "npm run check && npm run type-check"
  },
  "dependencies": {
    "astro": "^5.9.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.3"
  }
}
